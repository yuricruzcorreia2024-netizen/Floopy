(async()=>{try{const list=await loadCatalog();const hero=list[0];$('#hero').innerHTML=`<div class="hero-bg" style="background-image:url('${hero.banner||hero.cover}')"></div><div class="hero-content"><span class="eyebrow">DESTAQUE</span><h1>${esc(hero.title)}</h1><p>${esc(hero.description)}</p><div class="buttons"><a class="btn" href="anime.html?id=${hero.id}">▶ Assistir agora</a><a class="btn secondary" href="anime.html?id=${hero.id}">Detalhes</a></div></div>`;$('#popularGrid').innerHTML=list.slice(0,6).map(card).join('');$('#latestGrid').innerHTML=[...list].sort((a,b)=>b.year-a.year).map(card).join('')}catch(e){$('#hero').innerHTML='<div class="hero-content"><h1>Floppy</h1><p>Configure a URL da sua API em js/config.js e publique o projeto.</p></div>'}})();
