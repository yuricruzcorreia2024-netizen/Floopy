(async()=>{const id=params.get('id');try{const list=await loadCatalog();const a=list.find(x=>x.id===id);if(!a)throw Error();const total=Number(a.episodes)||0;const max=total||24;let eps='';for(let i=1;i<=max;i++)eps+=`<a class="episode" href="player.html?id=${encodeURIComponent(a.id)}&episode=${i}">EP ${i}</a>`;$('#animePage').innerHTML=`<section class="anime-hero"><div class="anime-poster"><img src="${a.cover}" alt="${esc(a.title)}"></div><div class="anime-copy"><span class="eyebrow">ANIME</span><h1>${esc(a.title)}</h1><div class="tags">${(a.genres||[]).map(g=>`<span class="tag">${esc(g)}</span>`).join('')}</div><p>${esc(a.description)}</p><p>${a.year} · ${esc(a.status)} · ${total?total+' episódios':'episódios disponíveis na API'}</p><div class="buttons"><a class="btn" href="player.html?id=${a.id}&episode=1">▶ Episódio 1</a></div></div></section><section class="season"><h2>Temporada 1</h2><div class="episode-list">${eps}</div></section>`}catch(e){$('#animePage').innerHTML='<div class="empty">Anime não encontrado no catálogo.</div>'}})();
